import React, { Component } from 'react';
import AddMediaTabPresenter from './AddMediaTabPresenter';

export default class AddMediaTabContainer extends Component{
  render() {
    return <AddMediaTabPresenter />;
  }
}