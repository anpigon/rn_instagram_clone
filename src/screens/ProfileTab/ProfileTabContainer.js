import React, { Component } from 'react';
import ProfileTabPresenter from './ProfileTabPresenter';

export default class ProfileTabContainer extends Component{
  render() {
    return <ProfileTabPresenter />;
  }
}